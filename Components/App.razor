@using System.Reflection;
@using Bellwood.AdminPortal.Auth;
@using Microsoft.AspNetCore.Components.Authorization
@using Microsoft.AspNetCore.Components.Routing

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link rel="stylesheet" href="bootstrap/bootstrap.min.css" />
    <link rel="stylesheet" href="app.css" />
    <link rel="stylesheet" href="css/bellwood.css" />
    @* Scoped-CSS bundle produced by the Blazor build from *.razor.css files.
       Served as a static web asset by the framework — NOT a file in wwwroot.
       Required for NavMenu.razor.css and MainLayout.razor.css scoped styles. *@
    <link rel="stylesheet" href="Bellwood.AdminPortal.styles.css" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet @rendermode="InteractiveServer" />
</head>
<body>
    <CascadingAuthenticationState>
        <Router AppAssembly="@typeof(App).Assembly">
            <Found Context="routeData">
                @{
                    bool requiresAuth = routeData.PageType
                        .GetCustomAttributes(inherit: true)
                        .Any(attr => attr is StaffAuthorizeAttribute);

                    if (requiresAuth)
                    {
                        <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(Layout.MainLayout)">
                            <NotAuthorized>
                                @* Inline redirect — avoids the missing RedirectToLogin component warning *@
                                <script>window.location.replace('/login');</script>
                            </NotAuthorized>
                        </AuthorizeRouteView>
                    }
                    else
                    {
                        var defaultLayout =
                            (routeData.PageType.GetCustomAttribute<LayoutAttribute>(inherit: true)?.LayoutType)
                            ?? typeof(Layout.MainLayout);

                        <RouteView RouteData="@routeData" DefaultLayout="@defaultLayout" />
                    }
                }
            </Found>
            <NotFound>
                <PageTitle>Not Found</PageTitle>
                <LayoutView Layout="@typeof(Layout.EmptyLayout)">
                    <div class="container mt-5">
                        <div class="alert alert-warning">
                            <h4>Page Not Found</h4>
                            <p>Sorry, there's nothing at this address.</p>
                            <a href="/login" class="btn btn-primary">Go to Login</a>
                        </div>
                    </div>
                </LayoutView>
            </NotFound>
        </Router>
    </CascadingAuthenticationState>

    <script src="_framework/blazor.web.js"></script>
    <script src="js/tracking-map.js"></script>
    <script src="js/utils.js"></script>
</body>
</html>

@code {
}
